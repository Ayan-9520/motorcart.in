/**
 * Central browser API entry — re-export Supabase client.
 * Future: add REST gateway, request IDs, and tracing here without touching feature modules.
 */
export { supabase } from "@/integrations/supabase/client";
